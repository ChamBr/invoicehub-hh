import { Card } from "@/components/ui/card";

export const PaymentIntegrations = () => {
  return (
    <Card className="p-6">
      <h2 className="text-xl font-semibold mb-4">Integrações de Pagamento</h2>
      {/* Implementação futura das integrações de pagamento */}
    </Card>
  );
};