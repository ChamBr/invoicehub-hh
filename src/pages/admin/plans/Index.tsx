import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const AdminPlans = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gerenciamento de Planos</CardTitle>
      </CardHeader>
      <CardContent>
        Conteúdo da página de gerenciamento de planos
      </CardContent>
    </Card>
  );
};

export default AdminPlans;